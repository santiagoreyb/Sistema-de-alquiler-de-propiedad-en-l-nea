CREATE TABLE Usuario(
id NUMBER(10, 0) GENERATED ALWAYS AS IDENTITY NOT NULL ENABLE,
nombre VARCHAR2(50) NOT NULL ENABLE,
contraseña VARCHAR2(20) NOT NULL ENABLE,
CONSTRAINT UUK_1 UNIQUE (nombre),
CONSTRAINT UPK_1 PRIMARY KEY (id)
)

CREATE TABLE Propiedad(
id NUMBER(10, 0) GENERATED ALWAYS AS IDENTITY NOT NULL ENABLE,
idUsuario NUMBER(10) NOT NULL ENABLE,
nombre VARCHAR2(50) NOT NULL ENABLE,
CONSTRAINT PRUK_1 UNIQUE (nombre),
CONSTRAINT PRPK_1 PRIMARY KEY (id),
CONSTRAINT PRFK_1 FOREIGN KEY (idUsuario) REFERENCES Usuario
)

CREATE TABLE Renta(
id NUMBER(10, 0) GENERATED ALWAYS AS IDENTITY NOT NULL ENABLE,
idPropiedad NUMBER(10) NOT NULL ENABLE,
fecha DATE DEFAULT sysdate NOT NULL ENABLE,
numTarjetaCredito NUMBER(20) NOT NULL ENABLE,
costoTotal NUMBER(20) DEFAULT 0 NOT NULL ENABLE,
CONSTRAINT RPK_1 PRIMARY KEY (id),
CONSTRAINT RFK_1 FOREIGN KEY (idPropiedad) REFERENCES Propiedad
)

CREATE TABLE Servicio(
id NUMBER(10, 0) GENERATED ALWAYS AS IDENTITY NOT NULL ENABLE,
nombre VARCHAR2(50) NOT NULL ENABLE,
precio NUMBER(10) NOT NULL ENABLE,
numInquilinos NUMBER(3) NOT NULL ENABLE,
costo NUMBER(10) DEFAULT 0 NOT NULL ENABLE,
CONSTRAINT SUK_1 UNIQUE (nombre),
CONSTRAINT SPK_1 PRIMARY KEY (id)
)

CREATE TABLE ServicioPorRenta(
idServicio NUMBER(10) NOT NULL ENABLE,
idRenta NUMBER(10) NOT NULL ENABLE,
CONSTRAINT SRPK_1 PRIMARY KEY (idServicio, idRenta),
CONSTRAINT SRFK_1 FOREIGN KEY (idServicio) REFERENCES Servicio,
CONSTRAINT SRFK_2 FOREIGN KEY (idRenta) REFERENCES Renta
)